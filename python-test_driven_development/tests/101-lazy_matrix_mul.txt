=================================
How to Use 101-lazy_matrix_mul.py
=================================

This module defines a matrix multiplication function ``lazy_matrix_mul(m_a, m_b)`` 
that multiplies two matrices using NumPy if available, with proper error handling.

Usage
=====

``lazy_matrix_mul(m_a, m_b)`` returns a new matrix representing the product
of ``m_a`` multiplied by ``m_b``.

Basic example:
::

    >>> lazy_matrix_mul = __import__('101-lazy_matrix_mul').lazy_matrix_mul
    >>> m_a = [
    ...     [1, 2],
    ...     [3, 4],
    ... ]
    >>> m_b = m_a
    >>> print(lazy_matrix_mul(m_a, m_b))
    [[7, 10],
     [15, 22]]

Non-square multiplication:
::

    >>> m_a = [[1, 2]]
    >>> m_b = [
    ...     [3, 4],
    ...     [5, 6]
    ... ]
    >>> print(lazy_matrix_mul(m_a, m_b))
    [[13, 16]]

Floating-point matrices:
::

    >>> m_a = [
    ...     [1.2, 5.5, 6.2],
    ...     [4.66, 12.3, -9.2]
    ... ]
    >>> m_b = [
    ...     [5.0, 3.3],
    ...     [-2.9, 4.4],
    ...     [7.2, 4.4]
    ... ]
    >>> print(lazy_matrix_mul(m_a, m_b))
    [[34.69, 55.44],
     [-78.61, 29.018]]

Argument Requirements
=====================

Two arguments must always be provided:
::

    >>> lazy_matrix_mul(m_a)
    TypeError: lazy_matrix_mul() missing 1 required positional argument: 'm_b'

::

    >>> lazy_matrix_mul()
    TypeError: lazy_matrix_mul() missing 2 required positional arguments: 'm_a' and 'm_b'

ValueErrors
===========

Cannot multiply matrices with incompatible shapes:
::

    >>> lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2], [2, 3], [4, 5]])
    ValueError: m_a and m_b can't be multiplied

Empty matrices:
::

    >>> lazy_matrix_mul([[]], [[5, 6], [7, 8]])
    ValueError: m_a and m_b can't be multiplied

::

    >>> lazy_matrix_mul([[5, 6], [7, 8]], [[]])
    ValueError: m_a and m_b can't be multiplied

Invalid Matrices
================

Inputs must be lists of lists of numbers. Otherwise:

Not lists:
::

    >>> lazy_matrix_mul("not a list", [[1, 2]])
    ValueError: Scalar operands are not allowed, use '*' instead

::

    >>> lazy_matrix_mul([[1, 2]], "not a list")
    ValueError: Scalar operands are not allowed, use '*' instead

Not list of lists:
::

    >>> lazy_matrix_mul([1, 2], [[3, 4]])
    ValueError: m_a and m_b can't be multiplied

::

    >>> lazy_matrix_mul([[1, 2]], [3, 4])
    ValueError: m_a and m_b can't be multiplied

Non-number elements:
::

    >>> lazy_matrix_mul([[1, "x"]], [[3, 4]])
    TypeError: m_a and m_b can't be multiplied

Unequal row lengths:
::

    >>> m_a = [[1, 2], [3, 4, 5]]
    >>> m_b = [[1, 2], [3, 4]]
    >>> lazy_matrix_mul(m_a, m_b)
    ValueError: m_a and m_b can't be multiplied
